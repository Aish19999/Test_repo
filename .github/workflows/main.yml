name: My Custom Workflow

on:
  push:
    branches:
      - main

jobs:
  my_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

  pipeline:
     uses: Aish19999/Reusable_workflow/.github/workflows/action.yml@main
